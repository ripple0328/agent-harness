# Private evidence, context, reviews, reports and backups stay local.
/.harness/

# Local dependencies and machine configuration.
node_modules/
.DS_Store
.env
.env.*
!.env.example
!.env.*.example

# Credentials and editor backups must never enter the publication set.
*.pem
*.key
*.p12
*.pfx
*~
*.bak
*.swp

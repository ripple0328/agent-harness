// This file is a traceability fixture, not a real product test suite.
// It demonstrates the @spec + ACn convention used by the harness.
// @spec example-feature

describe('example feature traceability fixture', () => {
  it('AC1 creates a named item for the user', () => {})
  it('AC2 rejects empty names', () => {})
  it('AC3 denies access to another user item', () => {})
})
